.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install pyome

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pyome
    $ pip install pyome
